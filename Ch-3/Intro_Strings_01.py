name = 'Sathvik'
short_name = name[0:3] #It start from 0 and goes till index 3 (excluding index 3 but including index 0)

print(short_name)

character = name[1]
print(character)

short_name_2 = name[-7:-1]
print(short_name_2)

print(name[:4])
print(name[0:4])

print(name[1:])
print(name[1:7])

# Strings are immuatable that means the single character of the string cannot be changed
